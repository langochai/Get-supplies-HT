using Newtonsoft.Json;
using System;
using System.Collections.Generic;
using System.Net.Http;
using System.Text;
using System.Threading.Tasks;

namespace ScanBarcode.Services
{
    public class MyHTTP
    {
        // Get new data rows
        //public static async void GetAllNewsAsync(Action<IEnumerable<Line>> action)
        //{

        //    HttpClient httpClient = new HttpClient();
        //    HttpResponseMessage response = await httpClient.GetAsync(Host.address + "material/listline");

        //    if (response.StatusCode == System.Net.HttpStatusCode.OK)
        //    {
        //        var list = JsonConvert.DeserializeObject<IEnumerable<Line>>(await response.Content.ReadAsStringAsync());
        //        action(list);
        //    }

        //}
    }
}
